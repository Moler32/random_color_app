import 'package:flutter/material.dart';

///localization class
class AppLocales {
  ///localization variables
  static const List<Locale> appLocales = [enLocale];

  ///localization variables
  static const Locale enLocale = Locale('en');
}
